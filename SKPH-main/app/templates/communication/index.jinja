<!DOCTYPE html>
<html>
<head>
    <title>Rejestracja</title>
</head>
<body>
    <h1>Witaj w naszym czacie!</h1>
    <form action="{{ url_for('chat.register') }}" method="post">
        <input type="text" name="username" placeholder="Nazwa użytkownika" required>
        <button type="submit">Zaloguj/Zarejestruj</button>
    </form>

    <h2>Zarejestrowani użytkownicy:</h2>
    <ul>
    {% for user in users %}
        <li>{{ user.username }}</li>
    {% endfor %}
    </ul>
</body>
</html>
